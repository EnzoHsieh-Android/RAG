spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: quentinz/bge-large-zh-v1.5:latest
        options:
          temperature: 0.7
    vector-store:
      qdrant:
        host: localhost
        port: 6333
        collection-name: rag-collection
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
